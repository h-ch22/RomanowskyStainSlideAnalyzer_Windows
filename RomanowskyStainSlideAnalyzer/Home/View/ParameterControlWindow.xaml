<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="RomanowskyStainSlideAnalyzer.Home.View.ParameterControlWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RomanowskyStainSlideAnalyzer.Home.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Customize Parameters (Advanced)">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" x:Name="AppTitleBar" Height="48" Margin="20,0,0,0" VerticalAlignment="Top" Padding="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <SymbolIcon Symbol="Setting"
                        x:Name="ic_titlebar"
                        HorizontalAlignment="Left" 
                        Grid.Column="0"
                        VerticalAlignment="Center"/>

            <TextBlock
                Margin="12,0,0,0"
                Grid.Column="1"
                VerticalAlignment="Center"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="Customize Parameters (Advanced)"/>
        </Grid>

        <CommandBar Grid.Row="1" Background="Transparent" DefaultLabelPosition="Right">
            <AppBarButton x:Name="btn_presets" Icon="Accept" Label="Presets"/>
            <AppBarButton x:Name="btn_reset" Icon="Refresh" Label="Reset to Default" Click="OnClick"/>
        </CommandBar>

        <ScrollView Margin="12 0" Grid.Row="2" >
            <StackPanel HorizontalAlignment="Stretch" Orientation="Vertical" x:Name="stackPanel">

                <Expander
                    IsExpanded="True"
                    ExpandDirection="Down"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch">
                    <Expander.Header>
                        <StackPanel Orientation="Vertical" Padding="0 10">
                            <TextBlock Text="Preset Name (Optional)" FontSize="12" Foreground="Gray"/>
                            <TextBox Margin="0 10" PlaceholderText="Preset Name" Text="{x:Bind presetName, Mode=TwoWay}"/>
                        </StackPanel>
                    </Expander.Header>

                    <Expander.Content>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="If you want to save your current settings and use them again later, name the Preset and click the Save button."/>
                            <Button x:Name="btn_save" Click="OnClick" Margin="0 10 0 0">Save</Button>
                        </StackPanel>
                    </Expander.Content>
                </Expander>
                
                <Expander
                    IsExpanded="False"
                    ExpandDirection="Down"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Margin="0 10">
                    <Expander.Header>
                        <StackPanel Orientation="Vertical" Padding="0 10">
                            <TextBlock Text="Points Per Side (Int)" FontSize="12" Foreground="Gray"/>
                            <NumberBox Margin="0 10" PlaceholderText="Points Per Side (Int)" Value="{x:Bind viewModel.PointsPerSide.Value, Mode=TwoWay}" SpinButtonPlacementMode="Inline" SmallChange="1" LargeChange="10" AcceptsExpression="True"/>
                        </StackPanel>
                    </Expander.Header>

                    <Expander.Content>
                        <TextBlock Text="{x:Bind viewModel.PointsPerSide.Description}"/>
                    </Expander.Content>
                </Expander>

                <Expander
                    IsExpanded="False"
                    ExpandDirection="Down"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Margin="0 10">
                    <Expander.Header>
                        <StackPanel Orientation="Vertical" Padding="0 10">
                            <TextBlock Text="Points Per Batch (Int)" FontSize="12" Foreground="Gray"/>
                            <NumberBox Margin="0 10" PlaceholderText="Points Per Batch (Int)" Value="{x:Bind viewModel.PointsPerBatch.Value, Mode=TwoWay}" SpinButtonPlacementMode="Inline" SmallChange="1" LargeChange="10" AcceptsExpression="True"/>
                        </StackPanel>
                    </Expander.Header>

                    <Expander.Content>
                        <TextBlock Text="{x:Bind viewModel.PointsPerBatch.Description}"/>
                    </Expander.Content>
                </Expander>

                <Expander
                    IsExpanded="False"
                    ExpandDirection="Down"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Margin="0 10">
                    <Expander.Header>
                        <StackPanel Orientation="Vertical" Padding="0 10">
                            <TextBlock Text="Pred IoU Thresh (Float)" FontSize="12" Foreground="Gray"/>
                            <NumberBox Margin="0 10" PlaceholderText="Pred Iou Thresh (Float)" Value="{x:Bind viewModel.PredIoUThresh.Value, Mode=TwoWay}" SpinButtonPlacementMode="Inline" SmallChange="0.1" LargeChange="1" AcceptsExpression="True"/>
                        </StackPanel>
                    </Expander.Header>

                    <Expander.Content>
                        <TextBlock Text="{x:Bind viewModel.PredIoUThresh.Description}"/>
                    </Expander.Content>
                </Expander>

                <Expander
                    IsExpanded="False"
                    ExpandDirection="Down"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Margin="0 10">
                    <Expander.Header>
                        <StackPanel Orientation="Vertical" Padding="0 10">
                            <TextBlock Text="Stability Score Thresh (Float)" FontSize="12" Foreground="Gray"/>
                            <NumberBox Margin="0 10" PlaceholderText="Stability Score Thresh (Float)" Value="{x:Bind viewModel.StabilityScoreThresh.Value, Mode=TwoWay}" SpinButtonPlacementMode="Inline" SmallChange="0.1" LargeChange="1" AcceptsExpression="True"/>
                        </StackPanel>
                    </Expander.Header>

                    <Expander.Content>
                        <TextBlock Text="{x:Bind viewModel.StabilityScoreThresh.Description}"/>
                    </Expander.Content>
                </Expander>

                <Expander
                    IsExpanded="False"
                    ExpandDirection="Down"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Margin="0 10">
                    <Expander.Header>
                        <StackPanel Orientation="Vertical" Padding="0 10">
                            <TextBlock Text="Stability Score Offset (Float)" FontSize="12" Foreground="Gray"/>
                            <NumberBox Margin="0 10" PlaceholderText="Stability Score Offset (Float)" Value="{x:Bind viewModel.StabilityScoreOffset.Value, Mode=TwoWay}" SpinButtonPlacementMode="Inline" SmallChange="0.1" LargeChange="1" AcceptsExpression="True"/>
                        </StackPanel>
                    </Expander.Header>

                    <Expander.Content>
                        <TextBlock Text="{x:Bind viewModel.StabilityScoreOffset.Description}"/>
                    </Expander.Content>
                </Expander>

                <Expander
                    IsExpanded="False"
                    ExpandDirection="Down"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Margin="0 10">
                    <Expander.Header>
                        <StackPanel Orientation="Vertical" Padding="0 10">
                            <TextBlock Text="Mask Threshold (Float)" FontSize="12" Foreground="Gray"/>
                            <NumberBox Margin="0 10" PlaceholderText="Mask Threshold (Float)" Value="{x:Bind viewModel.MaskThreshold.Value, Mode=TwoWay}" SpinButtonPlacementMode="Inline" SmallChange="0.1" LargeChange="1" AcceptsExpression="True"/>
                        </StackPanel>
                    </Expander.Header>

                    <Expander.Content>
                        <TextBlock Text="{x:Bind viewModel.MaskThreshold.Description}"/>
                    </Expander.Content>
                </Expander>

                <Expander
                    IsExpanded="False"
                    ExpandDirection="Down"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Margin="0 10">
                    <Expander.Header>
                        <StackPanel Orientation="Vertical" Padding="0 10">
                            <TextBlock Text="Box NMS Thresh (Float)" FontSize="12" Foreground="Gray"/>
                            <NumberBox Margin="0 10" PlaceholderText="Box NMS Thresh (Float)" Value="{x:Bind viewModel.BoxNMSThresh.Value, Mode=TwoWay}" SpinButtonPlacementMode="Inline" SmallChange="0.1" LargeChange="1" AcceptsExpression="True"/>
                        </StackPanel>
                    </Expander.Header>

                    <Expander.Content>
                        <TextBlock Text="{x:Bind viewModel.BoxNMSThresh.Description}"/>
                    </Expander.Content>
                </Expander>

                <Expander
                    IsExpanded="False"
                    ExpandDirection="Down"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Margin="0 10">
                    <Expander.Header>
                        <StackPanel Orientation="Vertical" Padding="0 10">
                            <TextBlock Text="Crop N Layers (Int)" FontSize="12" Foreground="Gray"/>
                            <NumberBox Margin="0 10" PlaceholderText="Crop N Layers (Int)" Value="{x:Bind viewModel.CropNLayers.Value, Mode=TwoWay}" SpinButtonPlacementMode="Inline" SmallChange="1" LargeChange="10" AcceptsExpression="True"/>
                        </StackPanel>
                    </Expander.Header>

                    <Expander.Content>
                        <TextBlock Text="{x:Bind viewModel.CropNLayers.Description}"/>
                    </Expander.Content>
                </Expander>

                <Expander
                    IsExpanded="False"
                    ExpandDirection="Down"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Margin="0 10">
                    <Expander.Header>
                        <StackPanel Orientation="Vertical" Padding="0 10">
                            <TextBlock Text="Crop NMS Thresh (Float)" FontSize="12" Foreground="Gray"/>
                            <NumberBox Margin="0 10" PlaceholderText="Crop NMS Thresh (Float)" Value="{x:Bind viewModel.CropNMSThresh.Value, Mode=TwoWay}" SpinButtonPlacementMode="Inline" SmallChange="0.1" LargeChange="1" AcceptsExpression="True"/>
                        </StackPanel>
                    </Expander.Header>

                    <Expander.Content>
                        <TextBlock Text="{x:Bind viewModel.CropNMSThresh.Description}"/>
                    </Expander.Content>
                </Expander>

                <Expander
                    IsExpanded="False"
                    ExpandDirection="Down"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Margin="0 10">
                    <Expander.Header>
                        <StackPanel Orientation="Vertical" Padding="0 10">
                            <TextBlock Text="Crop Overlap Ratio (Float)" FontSize="12" Foreground="Gray"/>
                            <NumberBox Margin="0 10" PlaceholderText="Crop Overlap Ratio (Float)" Value="{x:Bind viewModel.CropOverlapRatio.Value, Mode=TwoWay}" SpinButtonPlacementMode="Inline" SmallChange="0.1" LargeChange="1" AcceptsExpression="True"/>
                        </StackPanel>
                    </Expander.Header>

                    <Expander.Content>
                        <TextBlock Text="{x:Bind viewModel.CropOverlapRatio.Description}"/>
                    </Expander.Content>
                </Expander>

                <Expander
                    IsExpanded="False"
                    ExpandDirection="Down"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Margin="0 10">
                    <Expander.Header>
                        <StackPanel Orientation="Vertical" Padding="0 10">
                            <TextBlock Text="Crop N Points Downscale Factor (Int)" FontSize="12" Foreground="Gray"/>
                            <NumberBox Margin="0 10" PlaceholderText="Crop N Points Downscale Factor (Int)" Value="{x:Bind viewModel.CropNPointsDownScaleFactor.Value, Mode=TwoWay}" SpinButtonPlacementMode="Inline" SmallChange="1" LargeChange="10" AcceptsExpression="True"/>
                        </StackPanel>
                    </Expander.Header>

                    <Expander.Content>
                        <TextBlock Text="{x:Bind viewModel.CropNPointsDownScaleFactor.Description}"/>
                    </Expander.Content>
                </Expander>

                <Expander
                    IsExpanded="False"
                    ExpandDirection="Down"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Margin="0 10">
                    <Expander.Header>
                        <StackPanel Orientation="Vertical" Padding="0 10">
                            <TextBlock Text="Min Mask Region Area (Int)" FontSize="12" Foreground="Gray"/>
                            <NumberBox Margin="0 10" PlaceholderText="Min Mask Region Area (Int)" Value="{x:Bind viewModel.MinMaskRegionArea.Value, Mode=TwoWay}" SpinButtonPlacementMode="Inline" SmallChange="1" LargeChange="10" AcceptsExpression="True"/>
                        </StackPanel>
                    </Expander.Header>

                    <Expander.Content>
                        <TextBlock Text="{x:Bind viewModel.MinMaskRegionArea.Description}"/>
                    </Expander.Content>
                </Expander>
            </StackPanel>
        </ScrollView>
    </Grid>
</Window>
