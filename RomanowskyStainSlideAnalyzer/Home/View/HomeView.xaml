<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="RomanowskyStainSlideAnalyzer.Home.View.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RomanowskyStainSlideAnalyzer.Home.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:RomanowskyStainSlideAnalyzer.Home.Models"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="7*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <CommandBar x:Name="commandBar" IsEnabled="False" Background="Transparent" DefaultLabelPosition="Right" Grid.Row="0">
            <AppBarButton Icon="Pictures" Label="Images (F1)">
                <AppBarButton.KeyboardAccelerators>
                    <KeyboardAccelerator Key="F1"/>
                </AppBarButton.KeyboardAccelerators>

                <AppBarButton.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem x:Name="btn_loadImage" Icon="Add" Text="Add Images" Click="OnMenuFlyoutClick">
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Key="O" Modifiers="Control"/>
                            </MenuFlyoutItem.KeyboardAccelerators>
                        </MenuFlyoutItem>

                        <MenuFlyoutItem x:Name="btn_deleteImage" IsEnabled="False" Icon="Delete" Text="Delete Image" Click="OnMenuFlyoutClick">
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Key="Delete"/>
                            </MenuFlyoutItem.KeyboardAccelerators>
                        </MenuFlyoutItem>

                        <MenuFlyoutItem x:Name="btn_clear" Icon="Cancel" Text="Clear" Click="OnMenuFlyoutClick" Visibility="Collapsed">
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Key="Escape"/>
                            </MenuFlyoutItem.KeyboardAccelerators>
                        </MenuFlyoutItem>

                        <MenuFlyoutItem x:Name="btn_save" Icon="Download" Text="Save Result" Click="OnMenuFlyoutClick" Visibility="Collapsed">
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Key="S" Modifiers="Control"/>
                            </MenuFlyoutItem.KeyboardAccelerators>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </AppBarButton.Flyout>
            </AppBarButton>

            <AppBarSeparator/>

            <AppBarButton Icon="Find" Label="Parameters (F2)">
                <AppBarButton.KeyboardAccelerators>
                    <KeyboardAccelerator Key="F2"/>
                </AppBarButton.KeyboardAccelerators>

                <AppBarButton.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem x:Name="btn_customizeParams" Icon="Setting" Text="Customize Parameters (Advanced)" Click="OnMenuFlyoutClick">
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Key="C" Modifiers="Control"/>
                            </MenuFlyoutItem.KeyboardAccelerators>
                        </MenuFlyoutItem>

                        <MenuFlyoutItem x:Name="btn_setParamsAll" Icon="RepeatAll" Text="Set All Parameters" Click="OnMenuFlyoutClick">
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Key="A" Modifiers="Control"/>
                            </MenuFlyoutItem.KeyboardAccelerators>
                        </MenuFlyoutItem>

                        <MenuFlyoutItem x:Name="btn_labeling" Icon="Caption" Text="Labeling" Visibility="{Binding LabelingBtnVisibility, Mode=TwoWay}" Click="OnMenuFlyoutClick">
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Key="L" Modifiers="Control"/>
                            </MenuFlyoutItem.KeyboardAccelerators>
                        </MenuFlyoutItem>

                        <MenuFlyoutItem x:Name="btn_selectPoint" Icon="Favorite" Text="Select Points" IsEnabled="False" Click="OnMenuFlyoutClick">
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Key="P" Modifiers="Control"/>
                            </MenuFlyoutItem.KeyboardAccelerators>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </AppBarButton.Flyout>
            </AppBarButton>

            <AppBarToggleButton x:Name="btn_useAutomaticSegmentation" Icon="ViewAll" Label="Use Automatic Segmentation (F3)" IsChecked="{Binding UseAutomaticSegmentation, Mode=TwoWay}">
                <AppBarToggleButton.KeyboardAccelerators>
                    <KeyboardAccelerator Key="F3"/>
                </AppBarToggleButton.KeyboardAccelerators>
            </AppBarToggleButton>

            <AppBarToggleButton x:Name="btn_usePostProcess" Icon="Find" Label="Use Post-Process (F4)" IsChecked="{Binding UsePostProcess, Mode=TwoWay}">
                <AppBarToggleButton.KeyboardAccelerators>
                    <KeyboardAccelerator Key="F4" Modifiers="Shift"/>
                </AppBarToggleButton.KeyboardAccelerators>
            </AppBarToggleButton>

            <AppBarToggleButton x:Name="btn_extractMasks" Icon="Pictures" Label="Extract Masks (F5)" IsChecked="{Binding ExtractMasks, Mode=TwoWay}">
                <AppBarToggleButton.KeyboardAccelerators>
                    <KeyboardAccelerator Key="F5" Modifiers="Shift"/>
                </AppBarToggleButton.KeyboardAccelerators>
            </AppBarToggleButton>

            <AppBarToggleButton x:Name="btn_extractBBoxes" Icon="Crop" Label="Extract Bounding Boxes (F6)" IsChecked="{Binding ExtractBBoxes, Mode=TwoWay}">
                <AppBarToggleButton.KeyboardAccelerators>
                    <KeyboardAccelerator Key="F6" Modifiers="Shift"/>
                </AppBarToggleButton.KeyboardAccelerators>
            </AppBarToggleButton>

            <AppBarToggleButton x:Name="btn_useParallel" Icon="Switch" Label="Use Parallel GPU (F7)" IsChecked="{Binding UseParallel, Mode=TwoWay}" IsEnabled="{Binding IsParallelEnabled, Mode=TwoWay}">
                <AppBarToggleButton.KeyboardAccelerators>
                    <KeyboardAccelerator Key="F7" Modifiers="Shift"/>
                </AppBarToggleButton.KeyboardAccelerators>
            </AppBarToggleButton>

            <AppBarSeparator/>

            <AppBarButton x:Name="btn_segment" Icon="Accept" Label="Segment (F8)" Click="OnClick">
                <AppBarButton.KeyboardAccelerators>
                    <KeyboardAccelerator Key="F8"/>
                </AppBarButton.KeyboardAccelerators>
            </AppBarButton>
        </CommandBar>

        <StackPanel x:Name="configurationView" Orientation="Vertical" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <SymbolIcon x:Name="ic_status" Symbol="Setting" Foreground="Gray"/>
            <TextBlock x:Name="InitializeText" Text="Romanowsky Stain Slide Analyzer is configuring your computer environment.&#x0a;Please wait." Foreground="Gray" TextAlignment="Center"/>

            <SymbolIcon x:Name="ic_error" Symbol="Cancel" Foreground="Gray" Visibility="Collapsed"/>
            <TextBlock x:Name="ErrorText" Text="There was a problem processing your request.&#x0a;Please reinstall the software or contact your administrator." Foreground="Gray" TextAlignment="Center" Visibility="Collapsed"/>
        </StackPanel>

        <StackPanel AllowDrop="True" Drop="imageView_DragEnter"  DragOver="imageView_DragOver" x:Name="imageView" Orientation="Vertical" Grid.Row="1" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel x:Name="imageTutorialView" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
                <SymbolIcon Symbol="Add" Foreground="Gray"/>
                <TextBlock Text="Please add or drag images to continue." Foreground="Gray" TextAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Vertical" Visibility="Collapsed" x:Name="selectedImagePanel" VerticalAlignment="Center" HorizontalAlignment="Stretch">
                <FlipView SelectedIndex="{x:Bind CurrentIndex, Mode=TwoWay}" ItemsSource="{x:Bind FilesToSegment, Mode=TwoWay}" VerticalAlignment="Center" HorizontalAlignment="Center" Height="350" Width="1010" Background="Transparent">
                    <FlipView.ItemTemplate>
                        <DataTemplate x:DataType="models:SegmentParameterDataModel">
                            <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <StackPanel Orientation="Horizontal">
                                    <Image x:Name="selectedImageView" Width="500" Height="350" Stretch="Uniform" Source="{x:Bind filePath, Mode=TwoWay}"/>
                                    <Image x:Name="resultImageView" Margin="10 0 0 0" Width="500" Height="350" Stretch="Uniform" Source="{x:Bind destination, Mode=TwoWay}"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </FlipView.ItemTemplate>
                </FlipView>

                <PipsPager x:Name="pipsPager" HorizontalAlignment="Center" Margin="0 10 0 0" NumberOfPages="{x:Bind FilesToSegment.Count, Mode=OneWay}" SelectedPageIndex="{x:Bind CurrentIndex, Mode=TwoWay}"/>

                <StackPanel Orientation="Horizontal" Margin="0 10 0 0" x:Name="segmentHelpPanel" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <SymbolIcon x:Name="segmentHelpIcon" Symbol="{Binding SegmentHelpIcon, Mode=TwoWay}" Foreground="Gray" Margin="0 10 0 0" VerticalAlignment="Center"/>
                    <TextBlock Margin="10 0 0 0" x:Name="segmentHelpText" Text="{Binding SegmentHelpText, Mode=TwoWay}" Foreground="Gray" TextAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>

            </StackPanel>


        </StackPanel>

        <StackPanel x:Name="progressView" Orientation="Vertical" Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center">
            <ProgressBar x:Name="progressBar" Value="{Binding Progress, Mode=TwoWay}" IsIndeterminate="{Binding Indeterminate}" Width="500" Minimum="0" Maximum="100"/>
            <TextBlock x:Name="txt_status" Margin="0 10 0 0" Text="{Binding StatusText, Mode=TwoWay}" Foreground="Gray" TextAlignment="Center"></TextBlock>

            <Button x:Name="btn_reboot" Click="OnClick" Visibility="Collapsed">Reboot</Button>
        </StackPanel>

        <Grid Margin="20 0" x:Name="controlsView" Grid.Row="2" Visibility="Collapsed" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="0.5*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="0.5*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <RadioButtons Grid.Column="0" Header="Output File Name">
                <RadioButton x:Name="radio_prefix" Content="Prefix" Checked="OnRadioButtonChecked"/>
                <RadioButton x:Name="radio_postfix" Content="Postfix" Checked="OnRadioButtonChecked" IsChecked="True"/>
                <RadioButton x:Name="radio_newName" Content="New Name" Checked="OnRadioButtonChecked"/>
            </RadioButtons>

            <AppBarSeparator Grid.Column="1"/>

            <StackPanel Orientation="Vertical" Grid.Column="2">
                <TextBlock Text="Device"/>

                <DropDownButton x:Name="dropdown_devices" Content="{Binding Device, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <DropDownButton.Flyout>
                        <MenuFlyout x:Name="deviceMenu" Placement="Bottom">
                            <MenuFlyoutItem Click="OnDeviceSelected" Text="Auto"/>
                            <MenuFlyoutItem Click="OnDeviceSelected" Text="CPU"/>
                        </MenuFlyout>
                    </DropDownButton.Flyout>
                </DropDownButton>

                <TextBlock x:Name="txt_deviceHelp" Text="Only NVIDIA GPUs are displayed." FontSize="12" Foreground="Gray"/>
            </StackPanel>

            <AppBarSeparator Grid.Column="3"/>

            <StackPanel Orientation="Vertical" Grid.Column="4">
                <TextBlock Text="File Name"/>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBox PlaceholderText="{Binding PlaceHolderText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Text="{Binding FileName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="30" Width="250"/>

                    <DropDownButton Visibility="Collapsed" x:Name="dropDown_extensions" Content="{Binding Extension, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <DropDownButton.Flyout>
                            <MenuFlyout Placement="Bottom">
                                <MenuFlyoutItem Click="OnMenuItemClick" Text="png"/>
                                <MenuFlyoutItem Click="OnMenuItemClick" Text="jpg"/>
                                <MenuFlyoutItem Click="OnMenuItemClick" Text="jpeg"/>
                            </MenuFlyout>
                        </DropDownButton.Flyout>
                    </DropDownButton>
                </StackPanel>

                <TextBlock Text="{Binding HelpText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Foreground="Gray"/>
            </StackPanel>

        </Grid>

    </Grid>
</Page>