<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="RomanowskyStainSlideAnalyzer.Home.View.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RomanowskyStainSlideAnalyzer.Home.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="7*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <CommandBar IsEnabled="False" Background="Transparent" DefaultLabelPosition="Right" Grid.Row="0">
            <AppBarButton Icon="Add" Label="Load Image"/>
            <AppBarButton Icon="Setting" Label="Customize Parameters (Advanced)"/>
            <AppBarToggleButton Icon="Find" Label="Use Post-Process" IsChecked="True"/>
            <AppBarButton Icon="Accept" Label="Segment"/>
        </CommandBar>

        <StackPanel Orientation="Vertical" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <SymbolIcon x:Name="ic_status" Symbol="Download" Foreground="Gray"/>
            <TextBlock x:Name="InitializeText" Text="Romanowsky Stain Slide Analyzer is checking your environment.&#x0a;Please wait." Foreground="Gray" TextAlignment="Center"/>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center">
            <ProgressBar x:Name="progressBar" Value="{Binding Progress, Mode=TwoWay}" Width="500" Minimum="0" Maximum="100"/>
            <TextBlock x:Name="txt_status" Margin="0 10 0 0" Text="{Binding StatusText, Mode=TwoWay}" Foreground="Gray" TextAlignment="Center"></TextBlock>
            <Button x:Name="btn_reboot" Click="OnClick" Visibility="Collapsed">Reboot</Button>
        </StackPanel>
        
    </Grid>
</Page>
