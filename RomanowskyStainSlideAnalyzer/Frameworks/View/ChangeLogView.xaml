<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="RomanowskyStainSlideAnalyzer.Frameworks.View.ChangeLogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RomanowskyStainSlideAnalyzer.Frameworks.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Release Note">

    <Grid x:Name="gridView">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" x:Name="AppTitleBar" Height="48" Margin="20,0,0,0" VerticalAlignment="Top" Padding="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <SymbolIcon Symbol="Upload"
                        x:Name="ic_titlebar"
                        HorizontalAlignment="Left" 
                        Grid.Column="0"
                        VerticalAlignment="Center"/>

            <TextBlock
                Margin="12,0,0,0"
                Grid.Column="1"
                VerticalAlignment="Center"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="Release Note"/>
        </Grid>

        <TextBlock Text="{Binding Version}" Grid.Row="1" FontSize="18" Margin="20" FontWeight="SemiBold"/>

        <ListView
            Grid.Row="2"
            SelectionMode="None"
            ItemTemplate="{StaticResource ReleaseNoteItemTemplate}"
            BorderThickness="0"
            BorderBrush="{ThemeResource SystemControlTransparentBrush}"
            ItemsSource="{Binding ReleaseNotes, Mode=TwoWay}"
            HorizontalContentAlignment="Stretch"/>
    </Grid>
</Window>
