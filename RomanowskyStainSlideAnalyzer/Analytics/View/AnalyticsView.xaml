<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="RomanowskyStainSlideAnalyzer.Analytics.View.AnalyticsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RomanowskyStainSlideAnalyzer.Analytics.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:historyModel="using:RomanowskyStainSlideAnalyzer.History.Models"
    mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="LabeledDataItemTemplate" x:DataType="historyModel:LabeledHistoryDataModel">
                <ItemContainer AutomationProperties.Name="{x:Bind Id, Mode=TwoWay}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Image Width="120" Height="120" Grid.Column="0" Source="{x:Bind ImagePath, Mode=TwoWay}" Stretch="Uniform"/>

                        <TextBlock Margin="10 0 0 0" Grid.Column="1" Text="{x:Bind Id, Mode=TwoWay}"/>
                    </Grid>
                </ItemContainer>
            </DataTemplate>
        </ResourceDictionary>
    </Page.Resources>

    <ScrollViewer>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="First, select a labeling data" Foreground="Gray" HorizontalAlignment="Center" Margin="20"/>
            <ListView Grid.Row="1" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollMode="Auto" ScrollViewer.HorizontalScrollMode="Auto" SelectionMode="Single" ItemTemplate="{StaticResource LabeledDataItemTemplate}" Visibility="Collapsed" x:Name="listView"/>
            <ProgressRing IsIndeterminate="True" Visibility="{x:Bind ShowProgress, Mode=TwoWay}" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="1"/>

            <Button Margin="20" HorizontalAlignment="Center" Width="200" Grid.Row="2" Foreground="{StaticResource TextOnAccentFillColorPrimary}" Background="{StaticResource SystemAccentColor}">Next</Button>
        </Grid>
    </ScrollViewer>
    

</Page>
