<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="RomanowskyStainSlideAnalyzer.History.View.HistoryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RomanowskyStainSlideAnalyzer.History.View"
    xmlns:model="using:RomanowskyStainSlideAnalyzer.History.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="HistoryListItemTemplate" x:DataType="model:HistoryDataModel">
            <ItemContainer AutomationProperties.Name="{x:Bind date, Mode=TwoWay}">
                <Grid Width="350" Background="{StaticResource SystemControlPageBackgroundAltHighBrush}" Padding="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Vertical" Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                        <Button HorizontalAlignment="Right" x:Name="btn_zoom">
                            <SymbolIcon Symbol="ZoomIn"/>

                            <Button.Flyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem Click="MenuFlyoutItem_Click" x:Name="btn_original" Text="Original Image">
                                        <MenuFlyoutItem.KeyboardAccelerators>
                                            <KeyboardAccelerator Key="O"/>
                                        </MenuFlyoutItem.KeyboardAccelerators>
                                    </MenuFlyoutItem>

                                    <MenuFlyoutItem Click="MenuFlyoutItem_Click" x:Name="btn_result" Text="Result Image">
                                        <MenuFlyoutItem.KeyboardAccelerators>
                                            <KeyboardAccelerator Key="R"/>
                                        </MenuFlyoutItem.KeyboardAccelerators>
                                    </MenuFlyoutItem>
                                </MenuFlyout>
                            </Button.Flyout>
                        </Button>
                        
                        <Image Width="256" Height="256" Source="{x:Bind imagePath, Mode=TwoWay}" Stretch="Uniform"/>
                        <TextBlock FontSize="15" FontWeight="SemiBold" TextAlignment="Center" HorizontalAlignment="Center" HorizontalTextAlignment="Center" Text="{x:Bind fileName, Mode=TwoWay}"/>
                        <TextBlock FontSize="12" TextAlignment="Center" HorizontalAlignment="Center" HorizontalTextAlignment="Center" Text="{x:Bind date, Mode=TwoWay}" Foreground="Gray"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 10 0 0">
                            <SymbolIcon Symbol="{x:Bind symbol, Mode=TwoWay}" Foreground="Gray"/>
                            <TextBlock Text="{x:Bind statusText, Mode=TwoWay}" Margin="5 0 0 0" Foreground="Gray" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel HorizontalAlignment="Center" Margin="0 10 0 0" Orientation="Horizontal" Grid.Row="1">
                        <Button Height="40" x:Name="btn_saveImage" Click="OnSaveImageClick" Foreground="{StaticResource TextOnAccentAAFillColorPrimary}" Background="{StaticResource SystemAccentColor}">
                            <StackPanel Orientation="Horizontal">
                                <SymbolIcon Symbol="Pictures"/>
                                <TextBlock Text="Save Image" Margin="10 0 0 0"/>
                            </StackPanel>
                        </Button>

                        <Button Margin="10 0 0 0" Height="40" x:Name="btn_showData" Click="OnLabelingOptionClick" Visibility="{x:Bind AnalyzeBtnVisibility, Mode=TwoWay}">
                            <StackPanel Orientation="Horizontal">
                                <SymbolIcon Symbol="AllApps"/>
                                <TextBlock Text="Analyze" Margin="10 0 0 0"/>
                            </StackPanel>
                        </Button>

                        <Button Margin="10 0 0 0" x:Name="btn_more" Height="40">
                            <Button.Content>
                                <SymbolIcon Symbol="More"/>
                            </Button.Content>

                            <Button.Flyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem Click="OnRelabelingOptionClick" x:Name="btn_relabel" Text="Re-labeling" Visibility="{x:Bind ReLabelingVisibility, Mode=TwoWay}">
                                        <MenuFlyoutItem.Icon>
                                            <SymbolIcon Symbol="Caption"/>
                                        </MenuFlyoutItem.Icon>

                                        <MenuFlyoutItem.KeyboardAccelerators>
                                            <KeyboardAccelerator Key="R"/>
                                        </MenuFlyoutItem.KeyboardAccelerators>
                                    </MenuFlyoutItem>

                                    <MenuFlyoutSubItem Text="Change Data">
                                        <MenuFlyoutSubItem.Icon>
                                            <SymbolIcon Symbol="MoveToFolder"/>
                                        </MenuFlyoutSubItem.Icon>

                                        <MenuFlyoutSubItem.KeyboardAccelerators>
                                            <KeyboardAccelerator Key="C"/>
                                        </MenuFlyoutSubItem.KeyboardAccelerators>

                                        <MenuFlyoutItem Click="ChangeLabelingData" x:Name="btn_changeLabelingData" Text="Change Labeling Data" Visibility="{x:Bind BBoxPropertiesVisibility, Mode=TwoWay}">
                                            <MenuFlyoutItem.KeyboardAccelerators>
                                                <KeyboardAccelerator Key="L"/>
                                            </MenuFlyoutItem.KeyboardAccelerators>
                                        </MenuFlyoutItem>

                                        <MenuFlyoutItem Click="ChangeLabelingData" x:Name="btn_changeLabelingMaskData" Text="Change Labeled Mask Data" Visibility="{x:Bind MaskPropertiesVisibility, Mode=TwoWay}">
                                            <MenuFlyoutItem.KeyboardAccelerators>
                                                <KeyboardAccelerator Key="C"/>
                                            </MenuFlyoutItem.KeyboardAccelerators>
                                        </MenuFlyoutItem>

                                        <MenuFlyoutItem Click="ChangeLabelingData" x:Name="btn_changeMaskData" Text="Change Mask Data">
                                            <MenuFlyoutItem.KeyboardAccelerators>
                                                <KeyboardAccelerator Key="M"/>
                                            </MenuFlyoutItem.KeyboardAccelerators>
                                        </MenuFlyoutItem>
                                    
                                    </MenuFlyoutSubItem>

                                    <MenuFlyoutSubItem Text="Save Data" Visibility="{x:Bind saveMenuVisibility, Mode=TwoWay}">
                                        <MenuFlyoutSubItem.Icon>
                                            <SymbolIcon Symbol="Download"/>
                                        </MenuFlyoutSubItem.Icon>

                                        <MenuFlyoutSubItem.KeyboardAccelerators>
                                            <KeyboardAccelerator Key="S"/>
                                        </MenuFlyoutSubItem.KeyboardAccelerators>

                                        <MenuFlyoutItem Click="OnSaveLabelingDataClick" x:Name="btn_saveLabelingData" Text="Save Labeling Data" Visibility="{x:Bind LabeledBBoxPropertiesVisibility, Mode=TwoWay}">
                                            <MenuFlyoutItem.KeyboardAccelerators>
                                                <KeyboardAccelerator Key="L"/>
                                            </MenuFlyoutItem.KeyboardAccelerators>
                                        </MenuFlyoutItem>

                                        <MenuFlyoutItem Click="OnSaveLabelingDataClick" x:Name="btn_saveMaskData" Text="Save Labeled Mask Data" Visibility="{x:Bind LabeledMaskPropertiesVisibility, Mode=TwoWay}">
                                            <MenuFlyoutItem.KeyboardAccelerators>
                                                <KeyboardAccelerator Key="M"/>
                                            </MenuFlyoutItem.KeyboardAccelerators>
                                        </MenuFlyoutItem>

                                        <MenuFlyoutItem Click="OnSaveLabelingDataClick" x:Name="btn_saveOriginalMask" Text="Save Original Mask Data" Visibility="{x:Bind MaskPropertiesVisibility, Mode=TwoWay}">
                                            <MenuFlyoutItem.KeyboardAccelerators>
                                                <KeyboardAccelerator Key="O"/>
                                            </MenuFlyoutItem.KeyboardAccelerators>
                                        </MenuFlyoutItem>

                                    </MenuFlyoutSubItem>
                                    
                                    <MenuFlyoutItem Click="SaveImageWithBBoxes" x:Name="btn_saveImageWithBBox" Text="Save Image with Bounding Boxes" Visibility="{x:Bind SaveBBoxImageVisibility, Mode=TwoWay}">
                                        <MenuFlyoutItem.Icon>
                                            <SymbolIcon Symbol="Crop"/>
                                        </MenuFlyoutItem.Icon>

                                        <MenuFlyoutItem.KeyboardAccelerators>
                                            <KeyboardAccelerator Key="B"/>
                                        </MenuFlyoutItem.KeyboardAccelerators>
                                    </MenuFlyoutItem>

                                    <MenuFlyoutItem Click="SaveWithLabeledMasks" x:Name="btn_saveImageWithMask" Text="Save Image with Masks" Visibility="{x:Bind MaskPropertiesVisibility, Mode=TwoWay}">
                                        <MenuFlyoutItem.Icon>
                                            <SymbolIcon Symbol="Pictures"/>
                                        </MenuFlyoutItem.Icon>

                                        <MenuFlyoutItem.KeyboardAccelerators>
                                            <KeyboardAccelerator Key="S"/>
                                        </MenuFlyoutItem.KeyboardAccelerators>
                                    </MenuFlyoutItem>

                                    <MenuFlyoutItem Click="Delete" x:Name="btn_delete" Text="Delete">
                                        <MenuFlyoutItem.Icon>
                                            <SymbolIcon Symbol="Delete"/>
                                        </MenuFlyoutItem.Icon>

                                        <MenuFlyoutItem.KeyboardAccelerators>
                                            <KeyboardAccelerator Key="Delete"/>
                                        </MenuFlyoutItem.KeyboardAccelerators>
                                    </MenuFlyoutItem>

                                    <MenuFlyoutItem Click="ShowLog" x:Name="btn_showLog" Text="Segmentation Info">
                                        <MenuFlyoutItem.Icon>
                                            <SymbolIcon Symbol="Setting"/>
                                        </MenuFlyoutItem.Icon>

                                        <MenuFlyoutItem.KeyboardAccelerators>
                                            <KeyboardAccelerator Key="P"/>
                                        </MenuFlyoutItem.KeyboardAccelerators>
                                    </MenuFlyoutItem>
                                </MenuFlyout>
                            </Button.Flyout>
                        </Button>
                    </StackPanel>
                </Grid>
            </ItemContainer>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton IsEnabled="False" x:Name="appBarProgress" Visibility="Collapsed">
                <AppBarButton.Content>
                    <ProgressRing IsIndeterminate="True"/>
                </AppBarButton.Content>
            </AppBarButton>

            <AppBarButton x:Name="btn_refresh" Click="Refresh" Label="Refresh" Icon="Refresh">
                <AppBarButton.KeyboardAccelerators>
                    <KeyboardAccelerator Key="F5"/>
                </AppBarButton.KeyboardAccelerators>
            </AppBarButton>
        </CommandBar>

        <CalendarDatePicker Grid.Row="1" Date="{x:Bind Date, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" x:Name="datePicker"/>

        <ProgressRing Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{x:Bind ShowProgress, Mode=TwoWay}" IsIndeterminate="True"/>

        <ScrollView Grid.Row="2" x:Name="listView" VerticalAlignment="Stretch" Margin="0 10 0 0">
            <GridView x:Name="historyListView"
                      ItemTemplate="{StaticResource HistoryListItemTemplate}"
                      IsItemClickEnabled="False"
                      SelectionMode="None"
                      Margin="10"/>
        </ScrollView>

        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center" x:Name="emptyPanel" Grid.Row="2">
            <SymbolIcon Symbol="Cancel" Foreground="Gray"/>
            <TextBlock Text="There are no records for the selected date." Foreground="Gray"/>
        </StackPanel>
    </Grid>
</Page>
